{
  "name": "king-ev-dashboard",
  "version": "1.0.0",
  "description": "King EV Static Web App",
  "scripts": {
    "dev": "serve public -p 3000",
    "build": "mkdir -p public && cp *.html *.js public/"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
